import styled from 'styled-components'
import { Layout } from 'antd'

const { Footer } = Layout

const AntdFooter = styled(Footer)`
  text-align: center;
`

export const FooterElements = {
  AntdFooter,
}
